interface:
  display_name: "WindClaw Session Sync"
  short_description: "Initialize WindClaw session sync and send session IDs to another OpenClaw through a Feishu group."
  default_prompt: "Use $windclaw-session-sync to initialize or repair WindClaw session sync for this Feishu group."